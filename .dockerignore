# Secrets — injected at runtime via compose env_file, never baked into the image
.env
.env.*
!.env.example

# VCS & tooling
.git
.gitignore
.github
.claude
.idea
.vscode

# Docker files themselves
Dockerfile
.dockerignore
docker-compose*.yml

# Build output & local artifacts
bin/
tmp/
vendor/
*.exe
*.out
*.test
*.log
coverage.*
*.coverprofile

# Docs that are not served at runtime
README.md
**/README.md
